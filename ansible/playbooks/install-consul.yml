---
# Playbook to install HashiCorp Consul on CentOS Stream 10 or Ubuntu 25.10
- name: Install HashiCorp Consul
  hosts: nomad
  become: true

  tasks:
    # CentOS/RHEL tasks
    - name: Install required packages (RHEL)
      ansible.builtin.dnf:
        name:
          - dnf-plugins-core
        state: present
      when: ansible_os_family == "RedHat"

    - name: Add HashiCorp repository (RHEL)
      ansible.builtin.yum_repository:
        name: hashicorp
        description: HashiCorp Stable - $basearch
        baseurl: https://rpm.releases.hashicorp.com/RHEL/$releasever/$basearch/stable
        enabled: true
        gpgcheck: true
        gpgkey: https://rpm.releases.hashicorp.com/gpg
      when: ansible_os_family == "RedHat"

    - name: Install Consul (RHEL)
      ansible.builtin.dnf:
        name: consul
        state: latest
      when: ansible_os_family == "RedHat"

    # Ubuntu/Debian tasks
    - name: Install required packages (Debian)
      ansible.builtin.apt:
        name:
          - gpg
          - curl
        state: present
        update_cache: true
      when: ansible_os_family == "Debian"

    - name: Add HashiCorp GPG key (Debian)
      ansible.builtin.apt_key:
        url: https://apt.releases.hashicorp.com/gpg
        state: present
      when: ansible_os_family == "Debian"

    - name: Add HashiCorp repository (Debian)
      ansible.builtin.apt_repository:
        repo: "deb [arch=amd64] https://apt.releases.hashicorp.com {{ ansible_distribution_release }} main"
        state: present
        filename: hashicorp
      when: ansible_os_family == "Debian"

    - name: Install Consul (Debian)
      ansible.builtin.apt:
        name: consul
        state: latest
        update_cache: true
      when: ansible_os_family == "Debian"

    # Common tasks
    - name: Create Consul data directory
      ansible.builtin.file:
        path: "{{ consul_data_dir }}"
        state: directory
        owner: consul
        group: consul
        mode: "0755"

    - name: Create Consul config directory
      ansible.builtin.file:
        path: "{{ consul_config_dir }}"
        state: directory
        owner: root
        group: root
        mode: "0755"
